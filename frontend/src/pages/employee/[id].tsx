import React from 'react';
import {useRouter} from "next/router";

const Page = () => {
    const router = useRouter()

    return (
        <div>

        </div>
    );
};

export default Page;


// {router.query.id}